{
  "policies": [
    {
      "name": "inject-every-org-api-key",
      "policyType": "custom-code-inbound",
      "handler": {
        "export": "default",
        "module": "$import(./modules/inject-api-key)"
      }
    }
  ]
}